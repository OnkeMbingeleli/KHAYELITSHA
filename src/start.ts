// TanStack Start global config entry point (auto-discovered by convention).
// Add request middleware here later if you need it (e.g. logging, headers).
import { createStart } from "@tanstack/react-start";

export const startInstance = createStart(() => {
  return {};
});
